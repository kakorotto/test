// This file can be replaced during build by using the `fileReplacements` array.
// For production environment

export const environment = {
  production: true,
  // Update this with your production backend URL
  // For example: 'https://your-backend-domain.com/api'
  // Or if backend is deployed separately, use that URL
  apiUrl: 'http://localhost:8080/api' // Default - update this with your backend URL
};

